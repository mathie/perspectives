<%= render 'shared/errors', model: inbox_perspective %>

<%= bootstrap_form_for inbox_perspective do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :description %>

  <%= f.select :show_items, InboxPerspective.show_items.map { |k, v| [k.humanize, k] } %>

  <%= f.submit %>
<% end %>