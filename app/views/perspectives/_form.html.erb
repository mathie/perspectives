<%= render 'shared/errors', model: perspective %>

<%= bootstrap_form_for perspective do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :description %>

  <%= yield f %>

  <%= f.submit %>
<% end %>