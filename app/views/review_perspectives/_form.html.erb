<%= render layout: 'perspectives/form', locals: { perspective: perspective } do |f| %>
  <%= f.select :show_projects, perspective.class.show_projects.map { |k, v| [k.humanize, k] } %>
  <%= f.select :show_items, perspective.class.show_items.map { |k, v| [k.humanize, k] } %>
  <%= f.select :sort_projects_by, perspective.class.sort_projects_bies.map { |k, v| [k.humanize, k] } %>
<% end %>
