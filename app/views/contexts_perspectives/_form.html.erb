<%= render layout: 'perspectives/form', locals: { perspective: perspective } do |f| %>
  <%= f.select :show_items, ContextsPerspective.show_items.map { |k, v| [k.humanize, k] } %>
  <%= f.select :sort_items_by, ContextsPerspective.sort_items_bies.map { |k, v| [k.humanize, k] } %>
<% end %>
